//2.-Determinar, de N cantidades, cuántas son menores o iguales a cero y cuántas mayores a cero
#include<stdio.h> // biblioteca
int main(){  //funcion principal
//Variables
int cero,mayores,menores,x,n,i;

//INICIO
printf("Ingrese la cantidad de numeros a evaluar: ");
scanf("%d",&n);
      
      i=0;      //Inicializar todos los que usemos como contadores para evitar datos basura
      cero=0;
      mayores=0;
      menores=0;
      
       for(i=1;i<=n;i++){     //utilice un ciclo for 
      	   printf("Ingrese una cantidad\n");
      	   scanf ("%d",&x);
      	        if(x==0){                //ademas de una estructura de seleccion anidada
      	        	cero=cero+1;
				  } 
				    else if(x>0){
				     	mayores=mayores+1;
				    }
				       else if (x<0){
				 	     menores=menores+1;
				   	 }
				   	   else {
				   	   	  printf("ERROR");
						  }
		}
      	  printf("El total de cantidades mayores a ceros son: %d\n",mayores);    //impresion de los resultados
      	   printf("El total de cantidades menores a ceros son:  %d\n",menores); 
      	    printf("El total de cantidades iguales a cero son: %d\n",cero); 
      	  
return 0;
}
