#include<stdio.h> //biblioteca
int main(){  //funcion principal
//variables
 int contador=2;
 
 //INICIO
 printf("Este programa muestra todos los numeros pares del 0 al 100: \n");
       while(contador<=100){     //uso de estrustura de repeticion
 	         printf("\n%d",contador);    //\n lo use para dar formato
 	         contador+=2;   //el contador va a umentando de 2 en 2
       }
return 0; 	
}
